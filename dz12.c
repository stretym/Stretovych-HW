#include <stdio.h>

int main() {
    // Визначення змінних
    double T;

    // Обчислення ланцюгового дробу
    T = 365 + 1 / (4 + (1 / (7 + 1) + 1) / 3);

    // Виведення результату
    printf("Наближений період обертання Землі навколо Сонця: %f днів\n", T);

    return 0;
}
